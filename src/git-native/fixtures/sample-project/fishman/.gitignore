# Local-only / generated
history/
logs/
cache/
temp/
.fishman/

# Secrets — never commit (except test fixtures under this tree)
*.secret.json
!environments/local.secret.json
.env.secret
.env.local
*.local.json
